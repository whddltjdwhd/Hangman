const word = document.querySelector(".showText");
const lives = document.querySelector(".showLives");
// console.log(word.textContent + lives.textContent);
for(let a of word.textContent) {
    console.log(a);
}
